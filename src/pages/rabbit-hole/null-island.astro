---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="Null Island | 🐇" description="0°N 0°E — population: undefined" favicon="🏝️">
  <div class="fixed inset-0 bg-gradient-to-b from-green-950/20 to-zinc-950"></div>
  
  <header class="fixed top-0 left-0 right-0 z-50 p-6 flex justify-between">
    <a href="/rabbit-hole" class="text-sm text-zinc-600 hover:text-green-400 transition-colors">← return to somewhere</a>
    <span class="text-xs text-zinc-700 font-mono">DEPTH: 1</span>
  </header>

  <main class="relative min-h-screen flex flex-col items-center justify-center px-6 py-24 z-10">
    <div class="max-w-lg text-center">
      <div class="text-6xl mb-4">🏝️</div>
      <h1 class="text-4xl font-light mb-2 text-green-400">null island</h1>
      <p class="text-zinc-600 text-sm mb-8 font-mono">0.0000°N, 0.0000°E</p>
      
      <div class="p-6 border border-zinc-800 rounded-lg bg-zinc-900/50 mb-8">
        <div class="grid grid-cols-2 gap-4 text-xs">
          <div class="text-left">
            <p class="text-zinc-600">Population:</p>
            <p class="text-green-400 font-mono">undefined</p>
          </div>
          <div class="text-left">
            <p class="text-zinc-600">Area:</p>
            <p class="text-green-400 font-mono">NaN km²</p>
          </div>
          <div class="text-left">
            <p class="text-zinc-600">Timezone:</p>
            <p class="text-green-400 font-mono">UTC±null</p>
          </div>
          <div class="text-left">
            <p class="text-zinc-600">Status:</p>
            <p class="text-green-400 font-mono">exists && !exists</p>
          </div>
        </div>
      </div>
      
      <p class="text-zinc-500 text-sm mb-8 leading-relaxed">
        Welcome to the place where all geocoding errors go to die. 
        Where coordinates become <span class="text-green-400">null</span>. 
        Where your GPS signal finally admits it has no idea where you are.
      </p>
      
      <div class="text-xs text-zinc-700 font-mono space-y-2 mb-8">
        <p>// Tourist attractions:</p>
        <p class="ml-4">• The Monument to Missing Data</p>
        <p class="ml-4">• Default Value Beach</p>
        <p class="ml-4">• The Floating Point Error Caves</p>
        <p class="ml-4">• undefined Café (always closed)</p>
      </div>
      
      <div class="flex flex-wrap justify-center gap-4">
        <a href="/rabbit-hole/signal-decay" class="px-4 py-2 text-xs border border-zinc-800 rounded hover:border-amber-500 hover:text-amber-400 transition-colors">lose signal →</a>
        <a href="/rabbit-hole/liminal" class="px-4 py-2 text-xs border border-zinc-800 rounded hover:border-red-500 hover:text-red-400 transition-colors">liminal.exe →</a>
      </div>
    </div>
    
    <div class="mt-12 text-xs text-zinc-800 font-mono">
      <p>if (location === null) welcome();</p>
    </div>
  </main>
  
  <div class="fixed bottom-4 right-4 text-[8px] text-zinc-800 font-mono">
    signal: <span id="signal">searching...</span>
  </div>
</BaseLayout>

<script>
  const signalEl = document.getElementById('signal');
  const signals = ['searching...', 'null', 'undefined', '404', 'lost', '???', 'found?', 'no'];
  
  setInterval(() => {
    if (signalEl) {
      signalEl.textContent = signals[Math.floor(Math.random() * signals.length)];
    }
  }, 1500);
</script>
